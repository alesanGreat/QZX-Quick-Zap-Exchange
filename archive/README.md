# Archive

This directory retains files that no longer belong to the active QZX runtime
but may still provide historical context.

`legacy-web-bootstrap/` contains the former root-level JavaScript manifest and
lockfile. The active website has its own dependency files under `WebsiteQZX/`.
