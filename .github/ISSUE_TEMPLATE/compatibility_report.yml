name: Compatibility report
description: Share reproducible evidence from an operating system or architecture.
title: "[Compatibility]: "
labels:
  - compatibility
body:
  - type: input
    id: version
    attributes:
      label: QZX version
      placeholder: 0.2.2.0.2
    validations:
      required: true
  - type: input
    id: environment
    attributes:
      label: Exact environment
      description: OS edition and version, Python, architecture, shell, and relevant dependencies.
    validations:
      required: true
  - type: input
    id: command
    attributes:
      label: Command and arguments
      description: Redact private paths, names, and values.
    validations:
      required: true
  - type: dropdown
    id: result
    attributes:
      label: Result
      options:
        - Worked as documented
        - Worked with a limitation
        - Failed
        - Could not complete the test
    validations:
      required: true
  - type: textarea
    id: evidence
    attributes:
      label: Reproduction and evidence
      description: Include exit code and sanitized human/JSON output when safe.
    validations:
      required: true
  - type: textarea
    id: limitations
    attributes:
      label: Limitations
      description: Permissions, optional dependencies, virtualization, untested branches, or other caveats.
