# QZX Commands Documentation

> This file has been automatically generated by generate_command_docs.py. Do not modify by hand.
> Generated on: 2026-07-30 17:16:21

## Table of Contents

- [Development Commands](#development-commands)
  - [addPythonDocstrings](#addpythondocstrings)
  - [analyzeComplexity](#analyzecomplexity)
  - [auditRepository](#auditrepository)
  - [cleanDevelopmentArtifacts](#cleandevelopmentartifacts)
  - [compareFiles](#comparefiles)
  - [deployProject](#deployproject)
  - [findDeadCode](#finddeadcode)
  - [formatCode](#formatcode)
  - [getGitStatus](#getgitstatus)
  - [getProjectTree](#getprojecttree)
  - [planProjectBootstrap](#planprojectbootstrap)
  - [prepareRelease](#preparerelease)
  - [projectDoctor](#projectdoctor)
  - [projectLanguages](#projectlanguages)
  - [scaffoldC](#scaffoldc)
  - [scaffoldCSharp](#scaffoldcsharp)
  - [scaffoldCpp](#scaffoldcpp)
  - [scaffoldGo](#scaffoldgo)
  - [scaffoldJava](#scaffoldjava)
  - [scaffoldJavaScript](#scaffoldjavascript)
  - [scaffoldKotlin](#scaffoldkotlin)
  - [scaffoldPhp](#scaffoldphp)
  - [scaffoldPython](#scaffoldpython)
  - [scaffoldRust](#scaffoldrust)
  - [scaffoldTypeScript](#scaffoldtypescript)
  - [scanProject](#scanproject)
  - [traceCircularImports](#tracecircularimports)
  - [traceEnvVar](#traceenvvar)
- [File Commands](#file-commands)
  - [auditWorkspace](#auditworkspace)
  - [changePermissions](#changepermissions)
  - [copyPath](#copypath)
  - [countLines](#countlines)
  - [createDirectory](#createdirectory)
  - [createZip](#createzip)
  - [deletePath](#deletepath)
  - [detectFileType](#detectfiletype)
  - [downloadFile](#downloadfile)
  - [extractZip](#extractzip)
  - [findBrokenSymlinks](#findbrokensymlinks)
  - [findDuplicateFiles](#findduplicatefiles)
  - [findFiles](#findfiles)
  - [findText](#findtext)
  - [getFileHash](#getfilehash)
  - [getHumanLanguageStats](#gethumanlanguagestats)
  - [getProgrammingLanguageStats](#getprogramminglanguagestats)
  - [inspectImage](#inspectimage)
  - [isDirectoryEmpty](#isdirectoryempty)
  - [isFileBinary](#isfilebinary)
  - [isFileEmpty](#isfileempty)
  - [listFiles](#listfiles)
  - [movePath](#movepath)
  - [readFile](#readfile)
  - [repairWorkspace](#repairworkspace)
  - [touchFile](#touchfile)
- [Network Commands](#network-commands)
  - [checkDns](#checkdns)
  - [checkSslCertificate](#checksslcertificate)
  - [checkUrlStatus](#checkurlstatus)
  - [getNetworkConfig](#getnetworkconfig)
  - [testInternetSpeed](#testinternetspeed)
- [System Commands](#system-commands)
  - [about](#about)
  - [checkExecutable](#checkexecutable)
  - [checkSystemPath](#checksystempath)
  - [clearScreen](#clearscreen)
  - [explainFileWithGemini](#explainfilewithgemini)
  - [getCpuLoad](#getcpuload)
  - [getCurrentDateTime](#getcurrentdatetime)
  - [getCurrentDirectory](#getcurrentdirectory)
  - [getCurrentUser](#getcurrentuser)
  - [getDiskHealth](#getdiskhealth)
  - [getDiskSpace](#getdiskspace)
  - [getGpuInfo](#getgpuinfo)
  - [getRamInfo](#getraminfo)
  - [getStartupPrograms](#getstartupprograms)
  - [help](#help)
  - [inspectPort](#inspectport)
  - [isAdmin](#isadmin)
  - [killProcess](#killprocess)
  - [listCommands](#listcommands)
  - [listDiskDevices](#listdiskdevices)
  - [listProcesses](#listprocesses)
  - [listSystemServices](#listsystemservices)
  - [runDiagnosticCommand](#rundiagnosticcommand)
  - [runScript](#runscript)
  - [systemDoctor](#systemdoctor)
  - [systemInfo](#systeminfo)
  - [terminal](#terminal)
  - [testDiskSpeed](#testdiskspeed)
  - [version](#version)
  - [welcome](#welcome)

## Command maturity

Every executable command has a fail-closed lifecycle assessment. Planning and proof-of-concept work stays outside the public loader; Alpha, Beta, Release Candidate, Stable, Deprecated, and Retired describe the command contract independently from the QZX package release channel. Immutable release tags preserve each version's assessment.

## Development Commands

### addPythonDocstrings

Command to generate documentation templates for Python code

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Previews or adds generated docstring templates to functions, methods, and classes in one Python file

**Parameters:**
- `file_path`: Path to the Python file to process - Required
- `style`: Documentation style (google, numpy, sphinx) - Optional (default: `google`)
- `overwrite`: Whether to overwrite existing docstrings - Optional (default: `false`)
- `dry_run`: Preview changes without modifying the file - Optional (default: `true`)

**Examples:**
- `qzx addPythonDocstrings myfile.py`
  Preview Google-style docstring templates for myfile.py
- `qzx addPythonDocstrings myfile.py sphinx`
  Preview Sphinx-style docstring templates for myfile.py
- `qzx addPythonDocstrings myfile.py --dry-run false`
  Back up myfile.py, then add missing Google-style docstrings
- `qzx addPythonDocstrings myfile.py --overwrite --dry-run false`
  Back up myfile.py, then replace existing docstrings

**Details:**
Generates documentation templates for Python code

Args:
    file_path (str): Path to the Python file to process
    style (str, optional): Documentation style (google, numpy, sphinx)
    overwrite (bool, optional): Whether to overwrite existing docstrings
    dry_run (bool, optional): Preview changes without modifying the file

Returns:
    Dictionary with the result of the operation

---

### analyzeComplexity

Command to analyze code complexity metrics for development environments

This version uses the centralized recursive file finder utility.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Analyzes code complexity metrics for files or projects

**Parameters:**
- `file_path`: Path to the file or directory to analyze - Required
- `recursive`: Whether to recursively analyze directories: -r/--recursive for unlimited, -rN/--recursiveN for N levels - Optional (default: `false`)
- `detail_level`: Detail level: "detailed" or "summary" - Optional (default: `detailed`)

**Examples:**
- `qzx analyzeComplexity "main.py"`
  Analyze complexity of a single Python file
- `qzx analyzeComplexity "src/components" -r`
  Recursively analyze all code files in a directory
- `qzx analyzeComplexity "project/" -r2 --detail-level summary`
  Generate a summary report analyzing up to 2 levels deep

**Details:**
Analyzes code complexity of files or directories

Args:
    file_path: Path to file or directory to analyze
    recursive: Whether to recursively analyze directories: -r/--recursive for unlimited, -rN/--recursiveN for N levels
    detail_level: Report detail level (detailed or summary)

Returns:
    Analysis results as string

---

### auditRepository

Command to audit a repository for hardcoded secrets, large files, duplicate assets,
broken symlinks, .gitignore issues, license compliance, and basic vulnerability patterns.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Runs security and quality audits on a Git repository (secrets, large files, duplicates, .gitignore compliance, licenses)

**Parameters:**
- `path`: Path to the repository to audit (default: '.') - Optional (default: `.`)

**Examples:**
- `qzx auditRepository`
  Audit the current repository
- `qzx auditRepository C:/other/project`
  Audit project at specified path

**Details:**
Executes the repository audit

---

### cleanDevelopmentArtifacts

Identify and optionally remove known development-generated directories.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Finds development caches, dependency directories, and generated build artifacts; preview is the default

**Parameters:**
- `scan_path`: Directory to scan (defaults to the current working directory) - Optional (default: `.`)
- `dry_run`: Preview matching directories without deleting them - Optional (default: `true`)
- `max_depth`: Maximum directory depth to inspect (must be at least 1) - Optional (default: `4`)

**Examples:**
- `qzx cleanDevelopmentArtifacts`
  Preview generated development directories below the current directory
- `qzx cleanDevelopmentArtifacts . --dry-run false`
  Back up the current directory, then remove every matched generated directory

**Details:**
Scan for generated development directories and optionally remove them.

Args:
    scan_path (str): The starting directory path
    dry_run (bool): Whether to skip actual deletion
    max_depth (int): Traversal depth limit

Returns:
    Dictionary with results and details

---

### compareFiles

Compare two files and report their differences.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Compares text files line by line and binary files by exact bytes and SHA-256

**Parameters:**
- `file1`: Path to the first file - Required
- `file2`: Path to the second file - Required
- `mode`: Comparison mode: "full", "summary", or "percent" - Optional (default: `full`)
- `max_bytes`: Maximum allowed size of each file in bytes (defaults to 1 MiB) - Optional (default: `1048576`)

**Examples:**
- `qzx compareFiles "file1.py" "file2.py"`
  Compare two Python files and show every difference
- `qzx compareFiles "file1.txt" "file2.txt" "summary"`
  Show a summary of differences between two text files
- `qzx compareFiles "version1.js" "version2.js" "percent"`
  Show the similarity percentage between two JavaScript files
- `qzx compareFiles "image-a.png" "image-b.png"`
  Check whether two binary files are exactly equal by bytes and SHA-256

**Details:**
Compare two files and report their differences.

Args:
    file1: Path to the first file.
    file2: Path to the second file.
    mode: Comparison mode (full, summary, percent).
    max_bytes: Maximum allowed size of each file in bytes.

Returns:
    The comparison result in the requested format.

---

### deployProject

Promote one reviewed artifact to a remote path over SSH.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Previews or deploys one explicit artifact over SSH using a verified remote backup, SHA-256 validation, atomic promotion, health checks, and automatic rollback

**Parameters:**
- `target_host`: SSH destination in host or user@host form - Required
- `path`: Explicit local artifact directory to deploy; QZX never builds it or falls back to the project root - Optional (default: `.`)
- `target_path`: Absolute remote directory that will become the active release - Required
- `port`: SSH port - Optional (default: `22`)
- `ssh_key`: Optional local SSH private-key file - Optional
- `known_hosts`: Optional explicit OpenSSH known_hosts file used to verify the remote server identity - Optional
- `health_url`: HTTP(S) endpoint without credentials, query, or fragment; required for a live deployment and checked after promotion - Optional
- `health_expect`: Optional text that the health response body must contain - Optional
- `health_attempts`: Health-check attempts from 1 to 20 - Optional (default: `5`)
- `health_interval`: Seconds between health attempts, from 0 to 30 - Optional (default: `2.0`)
- `health_timeout`: Timeout per health attempt, from 0.1 to 30 seconds - Optional (default: `5.0`)
- `deployment_id`: Optional stable identifier for audit and recovery paths - Optional
- `dry_run`: Preview the exact artifact and remote paths without connecting - Optional (default: `true`)

**Examples:**
- `qzx deployProject --target-host deploy@example.test --path ./dist --target-path /srv/example/current`
  Inspects the artifact and previews every remote recovery path
- `qzx deployProject --target-host deploy@example.test --path ./dist --target-path /srv/example/current --health-url https://example.test/health --health-expect "ready" --dry-run false`
  Backs up the artifact, then performs a verified deployment

**Details:**
Build a deployment plan and optionally execute it.

---

### findDeadCode

Command to identify dead code (unused classes, functions, or exports) in a codebase.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Scans files for declared functions and classes, and identifies those with zero references in other files

**Parameters:**
- `scan_path`: Path to scan for files (defaults to current directory) - Optional (default: `.`)

**Examples:**
- `qzx findDeadCode`
  Scan the current directory for dead code
- `qzx findDeadCode "src/"`
  Scan the src/ directory

**Details:**
Executes dead code detection

Args:
    scan_path (str): Path to scan

Returns:
    Dictionary with dead code analysis details

---

### formatCode

Command to format source code files by auto-detecting the language
and invoking the matching formatter tool.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Formats source code files by auto-detecting language and invoking the right formatter

**Parameters:**
- `path`: File or directory to format - Required
- `language`: Force a specific language (python, javascript, typescript, rust, go, php, c, cpp). Auto-detected by default. - Optional (default: ``)
- `dry_run`: Check if files would be changed without writing (true/false) - Optional (default: `false`)

**Examples:**
- `qzx formatCode src/`
  Format all supported source files in src/
- `qzx formatCode src/main.py`
  Format a single Python file with black
- `qzx formatCode src/ python`
  Format only Python files in src/
- `qzx formatCode src/ --dry-run`
  Dry-run format to see which files would change

**Details:**
Formats source code files by auto-detecting language and invoking the appropriate formatter.

Args:
    path (str): File or directory to format
    language (str): Optional language override
    dry_run (str): If 'true', only check without writing changes

Returns:
    dict: Result summary

---

### getGitStatus

Command to retrieve comprehensive, structured information about a Git repository

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Provides a structured summary of the Git repository state (branch, remote, changes, recent commits)

**Parameters:**
- `repo_path`: Path to the directory to scan (defaults to the current working directory) - Optional (default: `.`)

**Examples:**
- `qzx getGitStatus`
  Show git status for the current directory
- `qzx getGitStatus C:/some/path`
  Show git status for the repository at C:/some/path

**Details:**
Retrieves Git repository details and status

Args:
    repo_path (str): The path to check

Returns:
    Dictionary containing Git status data

---

### getProjectTree

Command to visualize a directory structure as a clean text-based tree or JSON model, excluding heavy or private files.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Generates a clean visual directory tree (ASCII and JSON) excluding heavy directories like node_modules

**Parameters:**
- `dir_path`: Path to the directory to visualize (defaults to current directory) - Optional (default: `.`)
- `max_depth`: Maximum depth level of the tree (defaults to 2) - Optional (default: `2`)
- `exclude_dirs`: Comma-separated directory names to exclude (defaults to standard caches and configs) - Optional (default: `.git,node_modules,__pycache__,.venv,env,dist,build,artifacts,.next,.nuxt,.idea,.vscode`)
- `include_files`: Whether to list files as well as directories (true/false) - Optional (default: `true`)

**Examples:**
- `qzx getProjectTree`
  Show directory tree for the current folder up to 2 levels deep
- `qzx getProjectTree src 3`
  Show directory tree for the src folder up to 3 levels deep
- `qzx getProjectTree . 2 .git,node_modules false`
  Show directory tree excluding only .git and node_modules, showing folders only

**Details:**
Executes directory tree generation

Args:
    dir_path (str): The starting directory path
    max_depth (str/int): Maximum depth to traverse
    exclude_dirs (str, optional): Commas separated exclude folder list
    include_files (str/bool): Whether to include files in the tree

Returns:
    Dictionary containing ASCII tree text and JSON tree model

---

### planProjectBootstrap

Describe bootstrap work without writing files or running tools.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Builds a selectable project bootstrap plan without writing files, installing dependencies, creating secrets, or running migrations

**Parameters:**
- `path`: Project directory to inspect or plan - Optional (default: `.`)
- `tech`: Explicit stack: python, node, typescript, rust, php, or cpp. Required when manifests do not identify exactly one stack - Optional
- `components`: Comma-separated plan sections: structure, environment, dependencies, configuration, hooks, database, checks, or all - Optional (default: `all`)

**Examples:**
- `qzx planProjectBootstrap . --tech python`
  Plan every Python bootstrap component without writes
- `qzx planProjectBootstrap ./web --tech typescript --components structure,dependencies,checks`
  Plan selected TypeScript bootstrap components
- `qzx planProjectBootstrap ./existing-project`
  Detect one unambiguous stack from existing manifests

**Details:**
Method that executes the command

Args:
    *args: Positional arguments
    **kwargs: Keyword arguments

Returns:
    The result of the command execution

---

### prepareRelease

Prepare version metadata without committing, tagging, or publishing.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Plans a release metadata update and can atomically update one manifest plus CHANGELOG.md; it never builds, commits, tags, or publishes

**Parameters:**
- `bump`: Version increment: patch, minor, or major - Optional (default: `patch`)
- `path`: Project directory to prepare - Optional (default: `.`)
- `dry_run`: Preview the exact metadata changes without writing - Optional (default: `true`)
- `new_version`: Explicit target version; recommended for pre-releases and projects that do not use three-part SemVer - Optional
- `release_notes`: Reviewed changelog text; required when applying with update_changelog=true - Optional
- `update_changelog`: Prepend a reviewed entry to CHANGELOG.md - Optional (default: `true`)
- `require_clean_git`: Require the project to be a clean Git worktree before applying changes - Optional (default: `true`)
- `manifest`: Explicit supported manifest path relative to the project when automatic detection would be ambiguous - Optional

**Examples:**
- `qzx prepareRelease`
  Preview the next patch version and all release-preparation preconditions
- `qzx prepareRelease --new-version 2.0.0rc1 --release-notes "Release candidate with reviewed fixes" --dry-run false`
  Back up a clean project, then atomically prepare an explicit Python pre-release without committing or tagging

**Details:**
Build a deterministic plan and optionally apply its metadata edits.

---

### projectDoctor

Command to run a full diagnostic on a workspace's project health (VCS, tech stack, deps, env, tests, dead code, circular imports, large files)

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Inspects project health: detects tech stack, dependencies, environment configuration, Git state, test suites, circular imports, dead code, and large files

**Parameters:**
- `path`: Path to the project directory to diagnose (default: '.') - Optional (default: `.`)

**Examples:**
- `qzx projectDoctor`
  Run project health diagnosis for current directory
- `qzx projectDoctor C:/my/project`
  Diagnose project at specified path

**Details:**
Executes the project doctor analysis

---

### projectLanguages

Build a trustworthy, AI-ready profile of a project's code languages.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Profiles a project's source languages and supporting formats with line, file, and byte percentages

**Parameters:**
- `scan_path`: Project directory or source file to analyze (defaults to current directory) - Optional (default: `.`)

**Examples:**
- `qzx projectLanguages`
  Profile languages in the current project
- `qzx projectLanguages "src/"`
  Profile languages in the src/ directory

**Details:**
Analyze a project in one pass and return language composition metrics.

Percentages for the main composition are based on source code lines.
If all detected source files are empty, the command falls back to bytes
and then file counts while reporting that basis explicitly.

---

### scaffoldC

Command to generate a basic scaffolding for a C program.
Creates a new C project with standard directory structure and basic files.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a C program

**Parameters:**
- `project_name`: Name of the C project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `with_tests`: Whether to include testing scaffolding - Optional (default: `true`)
- `build_system`: Build system to use (make, cmake, or none) - Optional (default: `make`)

**Examples:**
- `qzx scaffoldC my_project`
  Creates a new C project with Makefile in the current directory
- `qzx scaffoldC my_project /path/to/dir true cmake`
  Creates a new C project with CMake and tests in the specified directory
- `qzx scaffoldC network_tool . false none`
  Creates a C project without tests or build system in the current directory

**Details:**
Creates a basic scaffolding for a C program

Args:
    project_name (str): Name of the C project to create
    path (str): Path where to create the project
    with_tests (str): Whether to include testing scaffolding
    build_system (str): Build system to use (make, cmake, or none)

Returns:
    Dictionary with the operation results and status

---

### scaffoldCSharp

Command to generate a basic scaffolding for a C# program.
Creates a new C# console project with .NET SDK and xUnit tests.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a C# program

**Parameters:**
- `project_name`: Name of the C# project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `with_tests`: Whether to include test scaffolding (xUnit) - Optional (default: `true`)
- `project_type`: Project type: console (default) - Optional (default: `console`)

**Examples:**
- `qzx scaffoldCSharp my_project`
  Creates a new C# console project named "my_project" in the current directory
- `qzx scaffoldCSharp my_project /path/to/dir true`
  Creates a new C# project with xUnit tests in the specified directory

**Details:**
Creates a basic scaffolding for a C# program

---

### scaffoldCpp

Command to generate a basic scaffolding for a C++ program.
Creates a new C++ project with standard directory structure and basic files.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a C++ program

**Parameters:**
- `project_name`: Name of the C++ project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `with_tests`: Whether to include testing scaffolding (using Catch2) - Optional (default: `true`)
- `build_system`: Build system to use (make, cmake, or none) - Optional (default: `cmake`)
- `cpp_standard`: C++ standard to use (11, 14, 17, 20) - Optional (default: `17`)

**Examples:**
- `qzx scaffoldCpp my_project`
  Creates a new C++ project using CMake in the current directory
- `qzx scaffoldCpp my_project /path/to/dir true make 20`
  Creates a new C++ project using Make, C++20 and tests in the specified directory
- `qzx scaffoldCpp util_library . false none 17`
  Creates a C++ project without tests or build system in the current directory using C++17

**Details:**
Creates a basic scaffolding for a C++ program

Args:
    project_name (str): Name of the C++ project to create
    path (str): Path where to create the project
    with_tests (str): Whether to include testing scaffolding
    build_system (str): Build system to use (make, cmake, or none)
    cpp_standard (str): C++ standard to use (11, 14, 17, 20)

Returns:
    Dictionary with the operation results and status

---

### scaffoldGo

Command to generate a basic scaffolding for a Go program.
Creates a new Go project with standard directory structure and basic files.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a Go program

**Parameters:**
- `project_name`: Name of the Go project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `with_tests`: Whether to include test scaffolding (Go native tests) - Optional (default: `true`)
- `module_path`: Go module path (default: github.com/user/{project_name}) - Optional (default: ``)

**Examples:**
- `qzx scaffoldGo my_project`
  Creates a new Go project named "my_project" in the current directory
- `qzx scaffoldGo my_project /path/to/dir true github.com/example/my_project`
  Creates a new Go project with custom module path in the specified directory
- `qzx scaffoldGo api_service . false`
  Creates a new Go project named "api_service" without tests

**Details:**
Creates a basic scaffolding for a Go program

Args:
    project_name (str): Name of the Go project to create
    path (str): Path where to create the project
    with_tests (str): Whether to include test scaffolding
    module_path (str): Go module path

Returns:
    Dictionary with the operation results and status

---

### scaffoldJava

Command to generate a basic scaffolding for a Java program.
Creates a new Java project with Maven and JUnit 5.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a Java program

**Parameters:**
- `project_name`: Name of the Java project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `with_tests`: Whether to include test scaffolding (JUnit 5) - Optional (default: `true`)
- `build_tool`: Build tool to use (maven) - Optional (default: `maven`)

**Examples:**
- `qzx scaffoldJava my_project`
  Creates a new Java project named "my_project" with Maven in the current directory
- `qzx scaffoldJava my_project /path/to/dir true`
  Creates a new Java project with tests in the specified directory

**Details:**
Creates a basic scaffolding for a Java program

---

### scaffoldJavaScript

Command to generate a basic scaffolding for a JavaScript/Node.js program.
Creates a new JavaScript project with standard directory structure and basic files.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a JavaScript/Node.js program

**Parameters:**
- `project_name`: Name of the JavaScript project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `with_tests`: Whether to include test scaffolding (jest) - Optional (default: `true`)

**Examples:**
- `qzx scaffoldJavaScript my_js_project`
  Creates a new JavaScript project named "my_js_project" in the current directory
- `qzx scaffoldJavaScript backend_api /path/to/dir true`
  Creates a new JavaScript project with Jest tests in the specified directory

**Details:**
Creates a basic scaffolding for a JavaScript program

Args:
    project_name (str): Name of the JavaScript project to create
    path (str): Path where to create the project
    with_tests (str): Whether to include test scaffolding

Returns:
    Dictionary with the operation results and status

---

### scaffoldKotlin

Command to generate a basic scaffolding for a Kotlin program.
Creates a new Kotlin project with Gradle (Kotlin DSL) and JUnit 5.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a Kotlin program

**Parameters:**
- `project_name`: Name of the Kotlin project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `with_tests`: Whether to include test scaffolding (JUnit 5) - Optional (default: `true`)

**Examples:**
- `qzx scaffoldKotlin my_project`
  Creates a new Kotlin project named "my_project" with Gradle in the current directory
- `qzx scaffoldKotlin backend_api /path/to/dir true`
  Creates a new Kotlin project with tests in the specified directory

**Details:**
Creates a basic scaffolding for a Kotlin program

---

### scaffoldPhp

Command to generate a basic scaffolding for a PHP/Composer program.
Creates a new PHP project with standard directory structure and basic files.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a PHP/Composer program

**Parameters:**
- `project_name`: Name of the PHP project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `with_tests`: Whether to include test scaffolding (phpunit) - Optional (default: `true`)

**Examples:**
- `qzx scaffoldPhp my_php_project`
  Creates a new PHP project named "my_php_project" in the current directory
- `qzx scaffoldPhp app_service /path/to/dir true`
  Creates a new PHP project with PHPUnit tests in the specified directory

**Details:**
Creates a basic scaffolding for a PHP program

Args:
    project_name (str): Name of the PHP project to create
    path (str): Path where to create the project
    with_tests (str): Whether to include test scaffolding

Returns:
    Dictionary with the operation results and status

---

### scaffoldPython

Command to generate a basic scaffolding for a Python program.
Creates a new Python project with standard directory structure and basic files.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a Python program

**Parameters:**
- `project_name`: Name of the Python project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `with_tests`: Whether to include test scaffolding (pytest) - Optional (default: `true`)
- `create_venv`: Whether to create a virtual environment - Optional (default: `false`)

**Examples:**
- `qzx scaffoldPython my_project`
  Creates a new Python project named "my_project" in the current directory
- `qzx scaffoldPython my_project /path/to/dir true true`
  Creates a new Python project with tests and virtual environment in the specified directory
- `qzx scaffoldPython api_service . false`
  Creates a new Python project named "api_service" without tests in the current directory

**Details:**
Creates a basic scaffolding for a Python program

Args:
    project_name (str): Name of the Python project to create
    path (str): Path where to create the project
    with_tests (str): Whether to include test scaffolding
    create_venv (str): Whether to create a virtual environment

Returns:
    Dictionary with the operation results and status

---

### scaffoldRust

Command to generate a basic scaffolding for a Rust program.
Creates a new Rust project with standard directory structure and basic files.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a Rust program

**Parameters:**
- `project_name`: Name of the Rust project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `binary`: Whether to create a binary application (true) or a library (false) - Optional (default: `true`)
- `with_tests`: Whether to include test scaffolding - Optional (default: `true`)

**Examples:**
- `qzx scaffoldRust my_project`
  Creates a new Rust binary project named "my_project" in the current directory
- `qzx scaffoldRust my_library false`
  Creates a new Rust library project named "my_library" in the current directory
- `qzx scaffoldRust my_project /path/to/dir true false`
  Creates a new Rust binary project without tests in the specified directory

**Details:**
Creates a basic scaffolding for a Rust program

Args:
    project_name (str): Name of the Rust project to create
    path (str): Path where to create the project
    binary (str): Whether to create a binary application (true) or a library (false)
    with_tests (str): Whether to include test scaffolding

Returns:
    Dictionary with the operation results and status

---

### scaffoldTypeScript

Command to generate a basic scaffolding for a TypeScript program.
Creates a new TypeScript project with standard directory structure and basic files.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates a basic scaffolding for a TypeScript program

**Parameters:**
- `project_name`: Name of the TypeScript project to create - Required
- `path`: Path where to create the project (default: current directory) - Optional (default: `.`)
- `with_tests`: Whether to include test scaffolding (jest & ts-jest) - Optional (default: `true`)

**Examples:**
- `qzx scaffoldTypeScript my_ts_project`
  Creates a new TypeScript project named "my_ts_project" in the current directory
- `qzx scaffoldTypeScript backend_api_ts /path/to/dir true`
  Creates a new TypeScript project with Jest/ts-jest tests in the specified directory

**Details:**
Creates a basic scaffolding for a TypeScript program

Args:
    project_name (str): Name of the TypeScript project to create
    path (str): Path where to create the project
    with_tests (str): Whether to include test scaffolding

Returns:
    Dictionary with the operation results and status

---

### scanProject

Command to inspect and summarize a project workspace, identifying tech stacks, configuration setups, and missing env keys.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Scans a project root to identify technologies, scripts, configuration files, and missing environment keys

**Parameters:**
- `project_path`: Path to the project root directory (defaults to current directory) - Optional (default: `.`)

**Examples:**
- `qzx scanProject`
  Scan the current workspace directory
- `qzx scanProject C:/my-projects/app`
  Scan the project located at C:/my-projects/app

**Details:**
Scans and diagnoses a project directory

Args:
    project_path (str): Path to scan

Returns:
    Dictionary with project diagnostic details

---

### traceCircularImports

Command to detect circular import dependencies in Python projects.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Traces module imports recursively and identifies circular import dependencies (loops)

**Parameters:**
- `scan_path`: Path to scan for Python files (defaults to current directory) - Optional (default: `.`)

**Examples:**
- `qzx traceCircularImports`
  Search for circular imports in the current directory
- `qzx traceCircularImports "src/qzx"`
  Search for circular imports specifically inside src/qzx

**Details:**
Executes circular imports detection

Args:
    scan_path (str): Path to scan

Returns:
    Dictionary with circular import analysis details

---

### traceEnvVar

Command to trace environment variable usage across files, templates, and codebases.

**Category:** development
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Traces usage of an environment variable across code files, .env, and .env.example templates

**Parameters:**
- `var_name`: Name of the environment variable to search for (e.g. DATABASE_URL, PORT) - Required
- `project_path`: Path to the project root directory (defaults to current directory) - Optional (default: `.`)
- `recursive`: Whether to search directories recursively (defaults to True) - Optional (default: `true`)

**Examples:**
- `qzx traceEnvVar "PORT"`
  Trace usage of the PORT environment variable in the current directory
- `qzx traceEnvVar "STRIPE_API_KEY" "C:/my-project"`
  Trace usage of STRIPE_API_KEY in C:/my-project

**Details:**
Executes the trace environment variable diagnostic

Args:
    var_name (str): Name of environment variable
    project_path (str): Path to search
    recursive (bool/str): Recursive search setting

Returns:
    Dictionary with trace diagnostic details

---


## File Commands

### auditWorkspace

Describe cleanup candidates without changing the audited workspace.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Builds a deterministic, fingerprinted workspace cleanup plan without altering workspace contents

**Parameters:**
- `path`: Workspace directory to audit - Optional (default: `.`)
- `categories`: Comma-separated categories: build, temp, artifacts, duplicates, reorganizations - Optional (default: `build,temp,artifacts,duplicates,reorganizations`)
- `max_files`: Maximum number of non-directory entries to inspect - Optional (default: `15000`)
- `plan_file`: Optional new JSON file in which to save the plan; existing files are never overwritten - Optional

**Examples:**
- `qzx auditWorkspace .`
  Inspect the current workspace without writing files
- `qzx auditWorkspace . --categories build,temp,duplicates --plan-file qzx-repair-plan.json`
  Save a plan that repairWorkspace can validate later

**Details:**
Method that executes the command

Args:
    *args: Positional arguments
    **kwargs: Keyword arguments

Returns:
    The result of the command execution

---

### changePermissions

Command to change file or directory permissions (similar to chmod)

This version uses the centralized recursive file finder utility.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Changes permissions of a file or directory

**Parameters:**
- `path`: Path to the file or directory - Required
- `mode`: Permission mode in octal (e.g., 755) or string format (e.g., "a+x") - Required
- `recursive`: Whether to apply permissions recursively: -r/--recursive for unlimited, -rN/--recursiveN for N levels - Optional (default: `false`)

**Examples:**
- `qzx changePermissions myfile.txt 644`
  Change file permissions to 644 (rw-r--r--)
- `qzx changePermissions myscript.sh "a+x"`
  Make a script executable for all users
- `qzx changePermissions mydir 755 -r`
  Change directory permissions recursively
- `qzx changePermissions mydir 755 -r2`
  Change directory permissions up to 2 levels deep

**Details:**
Changes permissions of a file or directory

Args:
    path (str): Path to the file or directory
    mode (str/int): Permission mode in octal (e.g., 755) or string format (e.g., "a+x")
    recursive: Recursion level: none by default, -r/--recursive for unlimited, -rN/--recursiveN for N levels

Returns:
    Operation result

---

### copyPath

Copy one file, symlink, or directory to a separate path.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Copies one file, symbolic link, or directory to a separate destination with explicit depth and replacement behavior

**Parameters:**
- `source`: Path to the source file, symlink, or directory - Required
- `destination`: Path to the separate destination - Required
- `recursive`: For directories: omit or use -r to copy everything; use -rN for at most N levels; false/0 creates only the root - Optional
- `force`: Replace an existing destination after a safety backup - Optional (default: `false`)

**Examples:**
- `qzx copyPath source.txt destination.txt`
  Copy one regular file and verify it with SHA-256
- `qzx copyPath myfile.txt backup/myfile.txt`
  Copy one file into a backup directory
- `qzx copyPath sourcedir destinationdir -r`
  Copy a complete directory and preserve symlinks
- `qzx copyPath sourcedir destinationdir -r2`
  Copy a directory through two levels
- `qzx copyPath sourcedir destinationdir -r --force`
  Back up and replace an existing destination with a complete copy

**Details:**
Copy one entry after validating its complete path relationship.

---

### countLines

Command to count the number of lines in files with support for wildcards and recursive searching

Supports flags:
-r, -R, --recursive: Enable unlimited recursive directory search
-rN, --recursiveN: Enable recursive directory search up to N levels deep

This version uses the centralized recursive file finder utility.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Counts the number of lines in files with support for wildcards and recursive searching

**Parameters:**
- `file_path`: Path to the file(s) to count lines in. Supports wildcards like "*.py" - Required
- `recursive`: Recursion level: -r/--recursive for unlimited depth, -rN/--recursiveN for N levels deep - Optional
- `ignore_empty`: Whether to ignore empty lines when counting - Optional (default: `false`)

**Examples:**
- `qzx countLines "script.py"`
  Count the number of lines in a Python script
- `qzx countLines "data/logs.txt"`
  Count the number of lines in a log file
- `qzx countLines "*.py"`
  Count the number of lines in all Python files in the current directory
- `qzx countLines "src/**/*.js" -r`
  Count the number of lines in all JavaScript files in the src directory and its subdirectories
- `qzx countLines "*.txt" -r2 true`
  Count the number of non-empty lines in all text files in the current directory and subdirectories up to 2 levels deep

**Details:**
Counts the number of lines in files with support for wildcards and recursive searching

Args:
    file_path: Path to the file(s) to count lines in. Supports wildcards like "*.py"
    recursive: Recursion level: none by default, -r/--recursive for unlimited, -rN/--recursiveN for N levels
    ignore_empty: Whether to ignore empty lines when counting

Returns:
    Dictionary with line count results or error message

---

### createDirectory

Command to create one or more directories at specified paths

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates one or more directories at specified paths

**Parameters:**
- `directory_paths`: One or more paths where directories should be created - Required

**Examples:**
- `qzx createDirectory "ProjectFolder"`
  Create a single directory named "ProjectFolder"
- `qzx createDirectory "src/components" "src/styles" "src/utils"`
  Create multiple directories for a project structure

**Details:**
Creates one or more directories at the specified paths

Args:
    *directory_paths: One or more paths where directories should be created

Returns:
    Structured operation result

---

### createZip

Create a ZIP in a sibling staging file and commit it atomically.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Compresses a file or directory into an atomic ZIP archive with custom exclusion rules

**Parameters:**
- `zip_path`: Target path for the created ZIP archive (e.g. project.zip) - Required
- `source_path`: Local file or directory to compress - Required
- `exclude_patterns`: Comma-separated folders, files, or wildcards to exclude (defaults to standard caches) - Optional (default: `.git,node_modules,__pycache__,.venv,env,dist,build`)
- `overwrite`: Replace an existing ZIP after creating a safety backup - Optional (default: `false`)

**Examples:**
- `qzx createZip project.zip .`
  Compress the current directory into a new project.zip
- `qzx createZip src.zip src`
  Compress the src folder into a new src.zip
- `qzx createZip project.zip . --overwrite`
  Replace project.zip after creating a safety backup

**Details:**
Compress one source while preserving any prior destination.

---

### deletePath

Delete a file, symlink, or directory after a safety backup.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Previews or deletes a file or directory from the filesystem

**Parameters:**
- `target`: Path to the file, symlink, or directory - Required
- `recursive`: Use true/-r for all descendants or a positive integer for limited depth - Optional (default: `false`)
- `force`: Continue a limited-depth deletion after individual errors - Optional (default: `false`)
- `dry_run`: Preview the operation without deleting anything - Optional (default: `true`)
- `apply`: Explicitly authorize deletion; required together with dry_run=false - Optional (default: `false`)
- `allow_unsafe`: Allow deleting protected locations such as the current or home directory - Optional (default: `false`)

**Examples:**
- `qzx deletePath myfile.txt`
  Preview deletion of a file (default behavior)
- `qzx deletePath mydir --recursive true`
  Preview recursive deletion of a directory
- `qzx deletePath mydir --recursive true --dry_run false --apply`
  Back up and delete a directory and all descendants

**Details:**
Method that executes the command

Args:
    *args: Positional arguments
    **kwargs: Keyword arguments

Returns:
    The result of the command execution

---

### detectFileType

Command to identify file type based on its magic number (file signature)

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Identifies file type based on its magic number (file signature) rather than extension

**Parameters:**
- `file_path`: Path to the file to analyze - Required
- `detailed_info`: Whether to show detailed MIME type information - Optional (default: `false`)

**Examples:**
- `qzx detectFileType image.jpg`
  Identify the real type of image.jpg based on its contents
- `qzx detectFileType unknown.bin true`
  Identify an unknown file with detailed MIME information

**Details:**
Identifies file type based on its magic number

Args:
    file_path (str): Path to the file to analyze
    detailed_info (bool): Whether to include detailed MIME type information

Returns:
    Dictionary with the result of the analysis

---

### downloadFile

Command to download a file from the Internet

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Downloads a file from the Internet (similar to 'wget' or 'curl' in Unix)

**Parameters:**
- `url`: URL of the file to download - Required
- `destination_path`: Path where to save the downloaded file - Required
- `show_progress`: Whether to show download progress - Optional (default: `true`)
- `timeout`: Maximum wait time in seconds - Optional (default: `30`)
- `overwrite`: Replace an existing destination after creating a safety backup - Optional (default: `false`)

**Examples:**
- `qzx downloadFile https://example.com/file.txt downloads/file.txt`
  Download a sample file
- `qzx downloadFile https://example.com/file.zip downloads/file.zip false`
  Download a file without showing progress
- `qzx downloadFile https://example.com/large-file.iso downloads/file.iso true 120`
  Download a large file with extended timeout
- `qzx downloadFile https://example.com/file.txt downloads/file.txt --overwrite`
  Replace an existing file after creating a safety backup

**Details:**
Downloads a file from the Internet

Args:
    url (str): URL of the file to download
    destination_path (str): Path where to save the downloaded file
    show_progress (bool, optional): Whether to show download progress
    timeout (int, optional): Maximum wait time in seconds
    overwrite (bool, optional): Whether to replace an existing file

Returns:
    Operation result

---

### extractZip

Extract a ZIP archive only after validating every member.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Extracts files from a ZIP archive after validating paths, types, limits, and destination conflicts

**Parameters:**
- `zip_path`: Path to the ZIP file to extract - Required
- `target_path`: Destination directory to extract files into (defaults to current directory) - Optional (default: `.`)
- `overwrite`: Replace conflicting destination entries after backing up the target directory - Optional (default: `false`)
- `max_files`: Maximum number of files accepted from the archive - Optional (default: `10000`)
- `max_total_size_mb`: Maximum total uncompressed size accepted, in mebibytes - Optional (default: `1024`)

**Examples:**
- `qzx extractZip project.zip`
  Extract project.zip without replacing existing files
- `qzx extractZip project.zip C:/extracted-app`
  Extract project.zip into C:/extracted-app
- `qzx extractZip project.zip C:/extracted-app --overwrite`
  Replace conflicts after creating a target-directory backup

**Details:**
Validate, stage, and commit extraction of one ZIP archive.

---

### findBrokenSymlinks

Command to identify symbolic links and junctions pointing to nonexistent targets.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Scans directories for broken symbolic links (pointing to nonexistent files or folders)

**Parameters:**
- `folder_path`: Directory to scan (defaults to current directory) - Optional (default: `.`)
- `max_depth`: Maximum depth level to walk directories recursively (defaults to 4) - Optional (default: `4`)

**Examples:**
- `qzx findBrokenSymlinks`
  Search for broken symlinks in the current directory
- `qzx findBrokenSymlinks C:/some/path`
  Search for broken symlinks in C:/some/path

**Details:**
Scans for broken symlinks

Args:
    folder_path (str): Starting folder
    max_depth (str/int): Folder depth limit

Returns:
    Dictionary with list of broken symlinks

---

### findDuplicateFiles

Command to recursively identify duplicate files inside a folder to optimize disk usage.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Scans a directory for identical files using size, SHA-256, and byte-for-byte verification

**Parameters:**
- `scan_path`: Path to start searching for duplicate files (defaults to current directory) - Optional (default: `.`)
- `min_size_kb`: Minimum file size in KB to consider for duplicates (defaults to 10) - Optional (default: `10`)
- `max_depth`: Maximum depth level to walk directories recursively (defaults to 4) - Optional (default: `4`)

**Examples:**
- `qzx findDuplicateFiles`
  Search for duplicates in the current directory (min 10KB)
- `qzx findDuplicateFiles C:/my-assets 0`
  Search for all duplicate files of any size inside C:/my-assets

**Details:**
Locates duplicate files

Args:
    scan_path (str): Root folder to scan
    min_size_kb (str/int): Size threshold to filter files
    max_depth (str/int): Folder depth limit

Returns:
    Dictionary containing duplicate file lists and disk reclaim details

---

### findFiles

Search for files without mixing in directory listings or content grep.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Finds files by name, depth, size, and modification date with structured metadata

**Parameters:**
- `search_path`: Directory where the search starts - Optional (default: `.`)
- `pattern`: File name glob such as *.txt or data-???.csv; defaults to all files - Optional (default: `*`)
- `recursive`: Search depth: -r for unlimited, false for this directory only, or -rN for N levels - Optional (default: `-r`)
- `min_size`: Minimum size, for example 500KB, 10MiB, or bytes - Optional
- `max_size`: Maximum size, for example 2GB, 20MiB, or bytes - Optional
- `modified_after`: Only files modified at or after YYYY-MM-DD, "today", or "yesterday" - Optional
- `modified_before`: Only files modified before YYYY-MM-DD, "today", or "yesterday" - Optional
- `exclude`: Comma-separated file-name globs to exclude - Optional
- `exclude_dirs`: Comma-separated directory-name globs to skip - Optional
- `sort_by`: Sort by path, name, size, or modified - Optional (default: `path`)
- `descending`: Reverse the selected sort order (true/false) - Optional (default: `false`)
- `limit`: Maximum number of files returned after sorting - Optional

**Examples:**
- `qzx findFiles . "*.py" --exclude-dirs ".git,.venv"`
  Find Python files recursively while skipping common metadata directories
- `qzx findFiles Downloads "*" --min-size 100MiB --sort-by size --descending true --limit 20`
  Return the 20 largest matching files of at least 100 MiB
- `qzx findFiles logs "*.log" --modified-after 2026-07-01 --recursive false`
  Find recently modified log files in one directory

**Details:**
Find files and return complete, consistently structured metadata.

---

### findText

Command to search for text patterns in files with advanced options

Supports flags:
-r, -R, --recursive: Enable unlimited recursive directory search
-rN, --recursiveN: Enable recursive directory search up to N levels deep

This version uses the centralized recursive file finder utility.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Searches for text patterns in files with advanced filtering options

**Parameters:**
- `pattern`: Text pattern to search for (supports regular expressions) - Required
- `target`: File or directory (or space-separated list of files/directories) to search in - Required
- `recursive`: Recursion level: -r/--recursive for unlimited depth, -rN/--recursiveN for N levels deep - Optional
- `regex`: Use regular expressions for pattern matching (true/false) - Optional (default: `false`)
- `case_sensitive`: Perform case-sensitive search (true/false) - Optional (default: `true`)
- `file_pattern`: Only search in files matching this pattern (e.g., "*.py" or "*.{py,js}") - Optional (default: `*`)
- `context_lines`: Number of lines to show before and after each match - Optional (default: `0`)
- `invert_match`: Show lines that do NOT match the pattern (true/false) - Optional (default: `false`)
- `count_only`: Only show count of matches per file (true/false) - Optional (default: `false`)
- `max_matches`: Maximum number of matches to display (0 for unlimited) - Optional (default: `0`)
- `colored`: Highlight matches with color (true/false) - Optional (default: `true`)

**Examples:**
- `qzx findText "function" "script.js"`
  Find all occurrences of "function" in a JavaScript file
- `qzx findText "error" "logs" true`
  Find all occurrences of "error" in all files under logs directory
- `qzx findText "def\s+\w+" "src" true true`
  Find all function definitions in Python files using regex
- `qzx findText "WARNING|ERROR" "logs" true true false "*.log"`
  Find warnings or errors in log files
- `qzx findText "TODO" "src" true false true "*.{py,js,ts}" 2`
  Find TODOs in source files with 2 lines of context
- `qzx findText "function" "file1.js file2.js lib/utils.js"`
  Find all occurrences of "function" in multiple specific files

**Details:**
Searches for text patterns in files with advanced options

Args:
    pattern (str): Text pattern to search for
    target (str): File or directory to search in (space-separated for multiple)
    recursive: Recursion level: none by default, -r/--recursive for unlimited, -rN/--recursiveN for N levels
    regex (bool): Whether to use regular expressions for pattern matching
    case_sensitive (bool): Whether the search should be case-sensitive
    file_pattern (str): Only search in files matching this pattern (e.g., "*.py")
    context_lines (int): Number of lines to show before and after each match
    invert_match (bool): Show lines that do NOT match the pattern
    count_only (bool): Only show count of matches per file
    max_matches (int): Maximum number of matches to display (0 for unlimited)
    colored (bool): Whether to colorize the output

Returns:
    Dictionary with search results

---

### getFileHash

Command to calculate cryptographic hash values of files

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Calculates cryptographic hashes (MD5, SHA-1, SHA-256) of a file

**Parameters:**
- `file_path`: Path to the file to hash - Required
- `algorithm`: Hash algorithm to use (sha256, sha1, md5) - Optional (default: `sha256`)

**Examples:**
- `qzx getFileHash file.txt`
  Calculate SHA-256 hash for file.txt
- `qzx getFileHash file.txt md5`
  Calculate MD5 hash for file.txt

**Details:**
Calculates the hash of a file

Args:
    file_path (str): Path to the file
    algorithm (str, optional): Cryptographic algorithm (sha256, sha1, md5)

Returns:
    Dictionary with hash results and metadata

---

### getHumanLanguageStats

Command to analyze a file or multiple files and determine the percentage of text in different languages,
intelligently filtering out programming code. Supports wildcards and recursive directory search.

Supports flags:
-r, -R, --recursive: Enable recursive directory search
-i, --ignore-comments: Ignore code comments when analyzing text

This version uses the centralized recursive file finder utility.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Analyzes files to estimate human-language distribution, with wildcard and recursive search support.

**Parameters:**
- `file_path`: Path to the file(s) to analyze. Supports wildcards like *.txt - Required
- `ignore_comments`: Whether to ignore code comments when analyzing text. Can use -i or --ignore-comments flag. - Optional (default: `false`)
- `min_word_length`: Minimum length of words to consider for language detection - Optional (default: `4`)
- `languages`: Comma-separated list of languages to detect (default: all available) - Optional
- `recursive`: Whether to search recursively in directories. Can use -r, -R, or --recursive flag. - Optional (default: `false`)
- `show_files_match`: Whether to show the list of files found. Can use --show_files_match flag. - Optional (default: `false`)

**Examples:**
- `qzx getHumanLanguageStats myfile.txt`
  Analyze language distribution in a text file
- `qzx getHumanLanguageStats code.py -i`
  Analyze a Python file, ignoring code comments using flag
- `qzx getHumanLanguageStats "*.md" false 3`
  Analyze all Markdown files in current directory
- `qzx getHumanLanguageStats "docs/*.txt" -i`
  Analyze text files in docs directory, ignoring comments
- `qzx getHumanLanguageStats "src/**/*.py" -r`
  Analyze all Python files recursively in src directory using -r flag
- `qzx getHumanLanguageStats "**/*.py" -r -i`
  Analyze all Python files recursively, ignoring comments, using flags
- `qzx getHumanLanguageStats "**/*.py" -r --show_files_match`
  Analyze all Python files recursively and show the list of files found

**Details:**
Execute the command to analyze language statistics in files

Args:
    file_path (str): Path to the file(s) to analyze, supporting wildcards
    ignore_comments (bool): Whether to ignore code comments when analyzing
    min_word_length (int): Minimum length for words to be considered
    languages (str): Comma-separated list of languages to detect
    recursive (bool): Whether to search recursively in directories
    show_files_match (bool): Whether to show the list of files found

Returns:
    dict: Command result with language statistics

---

### getProgrammingLanguageStats

Command to identify programming language in one or more files and get code statistics.
Supports wildcards and recursive directory searching.

Supports flags:
-r, -R, --recursive: Enable unlimited recursive directory search
-rN, --recursiveN: Enable recursive directory search up to N levels deep

This version uses the centralized recursive file finder utility.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Analyzes files to determine the programming language and provides statistics about the code. Supports wildcards and recursive directory search.

**Parameters:**
- `file_path`: Path to the file(s) to analyze. Supports wildcards like *.py - Required
- `detailed`: Whether to provide detailed statistics - Optional (default: `false`)
- `languages`: Comma-separated list of languages to detect (default: all available) - Optional
- `recursive`: Recursion level: -r/--recursive for unlimited depth, -rN/--recursiveN for N levels deep - Optional

**Examples:**
- `qzx getProgrammingLanguageStats script.py`
  Analyze a Python file to determine language statistics
- `qzx getProgrammingLanguageStats app.js true`
  Analyze a JavaScript file with detailed statistics
- `qzx getProgrammingLanguageStats code.cpp false "cpp,c,python"`
  Analyze a C++ file specifically checking for C++, C and Python patterns
- `qzx getProgrammingLanguageStats "*.js" -r`
  Analyze all JavaScript files in current directory and all subdirectories
- `qzx getProgrammingLanguageStats "src/**/*.py" -r`
  Analyze all Python files in src directory and subdirectories with detailed stats
- `qzx getProgrammingLanguageStats "*.py" true null -r2`
  Analyze all Python files in current directory and up to 2 levels of subdirectories with detailed stats

**Details:**
Analyzes one or more files to determine programming language and code statistics.
Supports wildcards and recursive directory search.

Args:
    file_path (str): Path to the file(s) to analyze. Supports wildcards like *.py
    detailed (bool, optional): Whether to include detailed statistics
    languages (str, optional): Comma-separated list of languages to detect
    recursive: Recursion level: none by default, -r/--recursive for unlimited, -rN/--recursiveN for N levels

Returns:
    Dictionary with analysis results for each file and aggregated statistics

---

### inspectImage

Command to inspect image metadata (format, width, height, size) using native header parsing.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Inspects image dimensions, format, and size natively from headers (supports PNG, JPEG, GIF, BMP)

**Parameters:**
- `image_path`: Path to the image file to inspect - Required

**Examples:**
- `qzx inspectImage image.png`
  Inspect dimensions and format of image.png

**Details:**
Inspects an image file

Args:
    image_path (str): File path

Returns:
    Dictionary containing image metadata

---

### isDirectoryEmpty

Command to check if a directory is empty

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Checks if a directory is empty (contains no files or subdirectories)

**Parameters:**
- `directory_path`: Path to the directory to check - Required
- `include_hidden`: Whether to include hidden files in the check - Optional (default: `false`)

**Examples:**
- `qzx isDirectoryEmpty /path/to/directory`
  Check if /path/to/directory is empty, ignoring hidden files
- `qzx isDirectoryEmpty /path/to/directory true`
  Check if /path/to/directory is empty, including hidden files

**Details:**
Checks if a directory is empty

Args:
    directory_path (str): Path to the directory to check
    include_hidden (bool, optional): Whether to include hidden files in the check

Returns:
    Dictionary with the result of the check

---

### isFileBinary

Command to check if a file is binary or plain text

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Analyzes a file to determine if it's binary or plain text

**Parameters:**
- `file_path`: Path to the file to analyze - Required
- `sample_size`: Size of the file sample to analyze (in bytes) - Optional (default: `8192`)
- `binary_threshold`: Percentage of non-text bytes required to classify as binary - Optional (default: `10`)

**Examples:**
- `qzx isFileBinary script.py`
  Check if script.py is a binary file
- `qzx isFileBinary image.jpg`
  Check if image.jpg is a binary file
- `qzx isFileBinary unknown.dat 4096 5`
  Check if unknown.dat is binary, using a 4KB sample and 5% threshold

**Details:**
Checks if a file is binary or plain text

Args:
    file_path (str): Path to the file to analyze
    sample_size (int): Size of the file sample to analyze (in bytes)
    binary_threshold (int): Percentage of non-text bytes required to classify as binary

Returns:
    Dictionary with the result of the analysis

---

### isFileEmpty

Command to check if a file is empty (has zero bytes)

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Checks if a file is empty (has zero bytes)

**Parameters:**
- `file_path`: Path to the file to check - Required
- `consider_whitespace`: Whether to consider whitespace-only files as empty - Optional (default: `false`)

**Examples:**
- `qzx isFileEmpty /path/to/file.txt`
  Check if file.txt is completely empty (zero bytes)
- `qzx isFileEmpty /path/to/file.txt true`
  Check if file.txt is empty or contains only whitespace

**Details:**
Checks if a file is empty

Args:
    file_path (str): Path to the file to check
    consider_whitespace (bool, optional): Whether to consider whitespace-only files as empty

Returns:
    Dictionary with the result of the check

---

### listFiles

Command to list files in a directory with support for wildcards and recursive searching

Supports flags:
-r, -R, --recursive: Enable unlimited recursive directory search
-rN, --recursiveN: Enable recursive directory search up to N levels deep

This version uses the centralized recursive file finder utility.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Lists files in a directory with support for wildcards and recursive searching

**Parameters:**
- `directory_path`: Path to the directory to list files from - Optional (default: `.`)
- `pattern`: File pattern to filter by (e.g., "*.txt", "doc*.pdf") - Optional (default: `*`)
- `recursive`: Recursion level: none by default, -r/--recursive for unlimited depth, -rN/--recursiveN for N levels deep - Optional

**Examples:**
- `qzx listFiles`
  List all files in the current directory
- `qzx listFiles C:\Documents`
  List all files in the C:\Documents directory (Windows)
- `qzx listFiles /home/user/documents`
  List all files in the /home/user/documents directory (Linux/Mac)
- `qzx listFiles . "*.py"`
  List all Python files in the current directory
- `qzx listFiles src "*.js" -r`
  List all JavaScript files in the src directory and all its subdirectories
- `qzx listFiles src "*.js" -r2`
  List all JavaScript files in the src directory and up to 2 levels of subdirectories

**Details:**
Lists files in a directory with support for wildcards and recursive searching

Args:
    directory_path (str): Path to the directory to list files from
    pattern (str): File pattern to filter by (e.g., "*.txt", "doc*.pdf")
    recursive: Recursion level: none by default, -r/--recursive for unlimited, -rN/--recursiveN for N levels

Returns:
    Dictionary with the list of files and metadata

---

### movePath

Move one file, symlink, or complete directory.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Moves or renames one file, symbolic link, or complete directory; partial directory moves are rejected

**Parameters:**
- `source`: Path to the source file, symlink, or directory - Required
- `destination`: Path to the new filesystem location - Required
- `force`: Replace an existing destination after a safety backup - Optional (default: `false`)

**Examples:**
- `qzx movePath source.txt destination.txt`
  Move or rename one file
- `qzx movePath myfile.txt archive/myfile.txt`
  Move one file into an archive directory
- `qzx movePath sourcedir destinationdir`
  Move one complete directory
- `qzx movePath sourcedir destinationdir --force`
  Back up and replace an existing destination with a complete directory

**Details:**
Move a complete filesystem entry and report its committed state.

---

### readFile

Command to read and display the content of a file

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Reads and displays the content of a file

**Parameters:**
- `file_path`: Path to the file to read - Required
- `max_lines`: Maximum number of lines to read (if not provided, reads the entire file) - Optional

**Examples:**
- `qzx readFile myfile.txt`
  Read the entire content of myfile.txt
- `qzx readFile myfile.txt 10`
  Read the first 10 lines of myfile.txt
- `qzx readFile "path with spaces/myfile.txt"`
  Read a file with spaces in the path

**Details:**
Reads and displays the content of a file

Args:
    file_path (str): Path to the file to read
    max_lines (int, optional): Maximum number of lines to read. If not provided, reads the entire file.

Returns:
    Dictionary with file content and metadata

---

### repairWorkspace

Apply a reviewed cleanup plan through staging and revalidation.

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Validates a saved auditWorkspace plan and applies only explicitly selected, unchanged cleanup actions

**Parameters:**
- `path`: Workspace directory that the saved plan audits - Optional (default: `.`)
- `plan_file`: JSON plan created by auditWorkspace - Optional
- `action_ids`: Comma-separated executable action IDs reviewed by the operator - Optional
- `dry_run`: Validate and preview without changing the workspace - Optional (default: `true`)
- `apply`: Explicitly authorize selected actions; requires dry_run=false - Optional (default: `false`)

**Examples:**
- `qzx repairWorkspace . --plan-file qzx-repair-plan.json`
  Validate the saved plan and list its executable actions
- `qzx repairWorkspace . --plan-file qzx-repair-plan.json --action-ids act-123,act-456 --dry-run false --apply`
  Back up the workspace, revalidate both actions, then apply them

**Details:**
Method that executes the command

Args:
    *args: Positional arguments
    **kwargs: Keyword arguments

Returns:
    The result of the command execution

---

### touchFile

Command to create an empty file or update the timestamp of an existing file

**Category:** file
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Creates an empty file or updates the timestamp of an existing file

**Parameters:**
- `path`: Path to the file to create or update - Required
- `create_dirs`: Whether to create parent directories if they do not exist - Optional (default: `false`)
- `content`: Content to write to the file - Optional (default: ``)

**Examples:**
- `qzx touchFile newfile.txt`
  Create an empty file or update its timestamp
- `qzx touchFile dir/newfile.txt true`
  Create a file and its parent directories if they do not exist
- `qzx touchFile file.txt false "Hello World"`
  Create a file with content or update an existing file

**Details:**
Creates an empty file or updates the timestamp of an existing file

Args:
    path (str): Path to the file to create or update
    create_dirs (bool, optional): Whether to create parent directories if they do not exist
    content (str, optional): Content to write to the file

Returns:
    Operation result

---


## Network Commands

### checkDns

Command to inspect and return all common DNS records for a specific domain name.

**Category:** network
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Queries A, AAAA, MX, TXT, NS, and CNAME DNS records for a given domain

**Parameters:**
- `domain`: Domain name to query (e.g. google.com) - Required

**Examples:**
- `qzx checkDns google.com`
  Get all DNS records for google.com

**Details:**
Queries DNS records for a domain

Args:
    domain (str): Domain to query

Returns:
    Dictionary with resolved DNS records

---

### checkSslCertificate

Fetch, decode, and analyze a server certificate.

**Category:** network
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Inspects certificate dates, hostname coverage, and trust-chain validation

**Parameters:**
- `host`: Hostname of the server to check (for example, example.com) - Required
- `port`: TLS port number - Optional (default: `443`)

**Examples:**
- `qzx checkSslCertificate example.com`
  Inspect and validate example.com's certificate
- `qzx checkSslCertificate expired.badssl.com 443`
  Inspect an expired certificate while reporting the trust failure

**Details:**
Method that executes the command

Args:
    *args: Positional arguments
    **kwargs: Keyword arguments

Returns:
    The result of the command execution

---

### checkUrlStatus

Command to verify connectivity and retrieve metadata for a target URL

**Category:** network
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Requests an HTTP(S) URL and reports its status code, response time, and basic headers.

**Parameters:**
- `url`: Target URL to check (e.g., https://api.github.com) - Required
- `timeout`: Request timeout in seconds (default: 5.0) - Optional (default: `5.0`)

**Examples:**
- `qzx checkUrlStatus https://api.github.com`
  Verify if GitHub API is accessible and check response times
- `qzx checkUrlStatus https://httpbin.org/status/404 3.0`
  Check a URL with a custom 3-second timeout

**Details:**
Executes an HTTP request to check URL status

Args:
    url (str): Target URL
    timeout (float/str, optional): Connection timeout in seconds

Returns:
    Dictionary with response metrics, headers, and online status

---

### getNetworkConfig

Command to retrieve detailed network status including active interfaces, gateways, public IP, and VPN status.

**Category:** network
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Displays comprehensive network status (interfaces, local IPs, DNS, public IP, VPN detection)

**Parameters:**
- `check_public`: Whether to fetch public IP and location info (true/false) - Optional (default: `true`)

**Examples:**
- `qzx getNetworkConfig`
  Get full network diagnostics including public IP and VPN status
- `qzx getNetworkConfig false`
  Get local network info only, skipping public IP resolution

**Details:**
Gathers network configuration info

Args:
    check_public (str/bool): Query public IP APIs

Returns:
    Dictionary with network configuration details

---

### testInternetSpeed

Measure a real HTTP stream without printing progress into command output.

**Category:** network
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Measures HTTP latency in milliseconds and bounded download throughput in Mbps and MiB/s

**Parameters:**
- `max_seconds`: Maximum download measurement duration, greater than 0 and no more than 30 seconds - Optional (default: `3.0`)

**Examples:**
- `qzx testInternetSpeed`
  Measure HTTP latency and download throughput for up to 3 seconds
- `qzx testInternetSpeed --max-seconds 5`
  Run the download measurement for up to 5 seconds

**Details:**
Run three latency samples and one bounded streaming download.

---


## System Commands

### about

Display the canonical QZX creator, maintainer, and license details.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Displays QZX product, creator, maintainer, and license details

**Parameters:**
None

**Examples:**
- `qzx about`
  Display QZX product and attribution details

**Details:**
Return the canonical installed-package identity.

---

### checkExecutable

Inspect PATH without executing the discovered program by default.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Locates an executable in the system PATH and optionally reads its conventional --version output

**Parameters:**
- `executable`: Executable name or explicit path to locate (for example: git, node, or docker) - Required
- `include_version`: Run the exact resolved executable once with --version; disabled by default - Optional (default: `false`)

**Examples:**
- `qzx checkExecutable git`
  Locate Git without executing it
- `qzx checkExecutable node --include-version`
  Locate Node.js and explicitly request its --version output

**Details:**
Locate an executable and run it only after explicit opt-in.

---

### checkSystemPath

Command to inspect PATH environment variable health, check for invalid or duplicate directories, and resolve command conflicts.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Diagnoses the system PATH variable, lists broken or duplicate folders, and locates all instances of a binary

**Parameters:**
- `binary_name`: Optional binary name to search for (e.g. python, node, git) - Optional (default: ``)

**Examples:**
- `qzx checkSystemPath`
  Inspect system PATH directories for duplicates, errors, and invalid folder paths
- `qzx checkSystemPath python`
  Diagnose PATH and list all physical locations of python executables, in order of execution precedence

**Details:**
Diagnoses PATH and optional binary locations

Args:
    binary_name (str, optional): Name of executable to find

Returns:
    Dictionary with PATH analysis and executable locations

---

### clearScreen

Command to clear the terminal screen

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Clears the terminal screen

**Parameters:**
None

**Examples:**
- `qzx clearScreen`
  Clear the terminal screen

**Details:**
Clear the terminal screen

Returns:
    Dictionary with the operation result

---

### explainFileWithGemini

Preview and optionally send bounded file samples to Google Gemini.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Previews and optionally sends bounded file samples to Google Gemini for explanation

**Parameters:**
- `file_path`: Path to the text file to explain - Required
- `sample_size`: Characters sampled from the beginning, middle, and end (10-100000; default: 500) - Optional (default: `500`)
- `model`: Gemini model to use; empty selects a compatible available model - Optional (default: ``)
- `custom_prompt`: Custom instruction sent to Gemini before the bounded sample - Optional (default: ``)
- `max_file_size_mb`: Maximum local file size accepted in mebibytes (1-100; default: 10) - Optional (default: `10`)
- `dry_run`: Preview exactly what kind of data would be shared without contacting Gemini - Optional (default: `true`)
- `apply`: Authorize the external request; requires dry_run=false - Optional (default: `false`)

**Examples:**
- `qzx explainFileWithGemini "path/to/file.txt"`
  Preview the bounded sample and external service without sending file content
- `qzx explainFileWithGemini "path/to/file.txt" 1000 "" "" 10 --dry-run false --apply`
  Send up to 1000 characters from each sampled section to Google Gemini
- `qzx explainFileWithGemini "path/to/file.txt" 500 "gemini-2.5-flash" "" 10 --dry-run false --apply`
  Use a specific available Gemini model after explicit authorization

**Details:**
Preview or perform one bounded, explicit Gemini analysis request.

---

### getCpuLoad

Command to get CPU load information

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Gets information about current CPU usage

**Parameters:**
- `interval`: Time interval (in seconds) to calculate CPU usage - Optional (default: `1.0`)

**Examples:**
- `qzx getCpuLoad`
  Get CPU usage information with 1-second interval
- `qzx getCpuLoad 0.5`
  Get CPU usage information with 0.5-second interval

**Details:**
Gets CPU load information

Args:
    interval (float, optional): Time interval (in seconds) to calculate CPU usage.
                               Defaults to 1 second.

Returns:
    Dictionary with CPU load information

---

### getCurrentDateTime

Return rich, timezone-aware local date and time information.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Reports the current local date and time with timezone, calendar, ISO-week, and timestamp details

**Parameters:**
- `output_format`: Presentation format: full, simple, or iso - Optional (default: `full`)

**Examples:**
- `qzx getCurrentDateTime`
  Show detailed local date, time, and calendar context
- `qzx getCurrentDateTime --output-format simple`
  Show a compact local date and time
- `qzx getCurrentDateTime --output-format iso`
  Show a timezone-aware ISO 8601 value

**Details:**
Capture the local clock once and return a consistent result.

---

### getCurrentDirectory

Shows the current directory with an immediate inventory by default.

The default response is intentionally useful to AI agents: besides the
path, it reports how many files and directories exist at that level,
without forcing a second listing command. Optional analysis performs one
recursive scan and reuses it for size, totals, extensions, and notable
files.

Symbolic links are counted but never followed, preventing cycles and
avoiding scans outside the current directory.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Shows the current working directory

**Parameters:**
- `full`: Show the full path (true) or only the directory name (false) - Optional (default: `true`)
- `size`: Recursively calculate logical directory size and descendant file/directory totals - Optional (default: `false`)
- `details`: Include a recursive analysis, immediate entry preview, extension summary, largest/recent files, permissions, and filesystem capacity - Optional (default: `false`)
- `limit`: Maximum entries in each details list (1-100, default: 10) - Optional (default: `10`)

**Examples:**
- `qzx getCurrentDirectory`
  Shows the current path and counts files/directories at that level
- `qzx getCurrentDirectory --size`
  Adds recursive size and descendant totals in one filesystem scan
- `qzx getCurrentDirectory --details --limit 20`
  Returns a rich directory analysis with up to 20 items per list
- `qzx getCurrentDirectory --full false`
  Shows only the current directory name as the displayed path

**Details:**
Show the current directory and a token-saving filesystem summary.

Args:
    full (bool): Display the full path instead of only its final name.
    size (bool): Recursively calculate logical size and descendant totals.
    details (bool): Add rich previews and recursive aggregate analysis.
    limit (int): Maximum entries in each details list, from 1 through 100.

Returns:
    A structured path and immediate inventory. Recursive fields are
    included only when ``size`` or ``details`` is requested.

---

### getCurrentUser

Command to get information about the currently logged in user

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Gets information about the currently logged in user

**Parameters:**
None

**Examples:**
- `qzx getCurrentUser`
  Get detailed information about the currently logged in user

**Details:**
Gets information about the currently logged in user

Returns:
    Dictionary with user information and operation status

---

### getDiskHealth

Return health or full S.M.A.R.T. data for one explicit disk.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Inspects one disk's S.M.A.R.T. health with smartctl and reports status flags without treating health warnings as command failures

**Parameters:**
- `disk`: Disk identifier without path separators (for example: sda, nvme0n1, disk0, or PhysicalDrive0) - Required
- `view`: Detail level: "health" for a summary or "full" for smartctl JSON - Optional (default: `health`)

**Examples:**
- `qzx getDiskHealth sda`
  Inspect the S.M.A.R.T. health of /dev/sda
- `qzx getDiskHealth PhysicalDrive0 --view full`
  Read full S.M.A.R.T. JSON for a Windows physical drive

**Details:**
Run one exact smartctl binary with bounded time and no shell.

---

### getDiskSpace

Command to get information about disk space usage

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Gets information about disk space usage

**Parameters:**
- `path`: Path to get disk information from. If not provided, all disks will be shown - Optional

**Examples:**
- `qzx getDiskSpace`
  Get information about all available disks
- `qzx getDiskSpace C:`
  Get information about the C: drive (Windows)
- `qzx getDiskSpace /home`
  Get information about the /home partition (Linux/Mac)

**Details:**
Gets disk space information for the specified path or all disks

Args:
    path (str, optional): Path to get information from. If not provided, all disks will be shown.

Returns:
    Dictionary with disk information and success status

---

### getGpuInfo

Return normalized GPU inventory and optional live NVIDIA metrics.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Inspects installed GPUs and reports normalized vendor, driver, memory, and available utilization details

**Parameters:**
- `detailed`: Request extended driver, memory, temperature, and utilization fields when the platform exposes them - Optional (default: `false`)

**Examples:**
- `qzx getGpuInfo`
  List detected GPUs with normalized vendor names
- `qzx getGpuInfo --detailed`
  Include available driver, memory, temperature, and utilization fields

**Details:**
Inspect the current platform and return one stable result.

---

### getRamInfo

Command to get information about system RAM

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Gets detailed information about system RAM usage

**Parameters:**
None

**Examples:**
- `qzx getRamInfo`
  Get detailed information about system memory usage

**Details:**
Gets information about system RAM

Returns:
    Dictionary with RAM information

---

### getStartupPrograms

Command to audit and list all startup programs configured to run on boot or login.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Lists all startup programs configured to run on system boot or user login

**Parameters:**
None

**Examples:**
- `qzx getStartupPrograms`
  Retrieve a list of all startup programs and registry triggers

**Details:**
Retrieves startup program list

Returns:
    Dictionary with startup applications details

---

### help

Muestra la ayuda para un comando específico o la ayuda general del sistema.
Proporciona información detallada sobre el uso, parámetros y ejemplos de comandos.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Shows help for a command

**Parameters:**
- `command`: Name of the command to get help for - Optional

**Examples:**
- `qzx help`
  Shows general help information
- `qzx help readFile`
  Shows detailed help for the readFile command
- `qzx readFile --help`
  Shows the same command help with the conventional flag

**Details:**
Muestra la ayuda para un comando específico o la ayuda general del sistema.

Args:
    command (str, optional): Nombre del comando para el que se quiere obtener ayuda

Returns:
    dict: Diccionario con la información de ayuda solicitada

---

### inspectPort

Inspect a bound local port without changing process state.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Checks whether a local port is bound and reports the owning process without terminating it

**Parameters:**
- `port`: Port number to inspect - Required

**Examples:**
- `qzx inspectPort 3000`
  Inspect the listener and obtain its PID and creation time
- `qzx inspectPort 5432 --json`
  Return the complete structured inspection result

**Details:**
Inspect a local port and report stable, structured ownership data.

---

### isAdmin

Command to check if the current user has administrative privileges

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Checks if the current user has administrative privileges

**Parameters:**
None

**Examples:**
- `qzx isAdmin`
  Check if the current user has administrative privileges

**Details:**
Checks if the current user has administrative privileges

Returns:
    Dictionary with administrative privilege information and status

---

### killProcess

Terminate one process by PID and verify that it exited.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Terminates one explicitly identified process and verifies that it exited

**Parameters:**
- `pid`: Positive process ID to terminate - Required
- `force`: Use an immediate forced kill instead of graceful termination - Optional (default: `false`)
- `expected_create_time`: Optional process creation timestamp observed immediately before termination; prevents PID-reuse mistakes - Optional
- `wait_seconds`: Seconds to wait for verified process exit (0.1 to 60) - Optional (default: `5.0`)

**Examples:**
- `qzx killProcess 1234 --expected-create-time 1750000000.25 --yolo`
  Terminate exactly the previously inspected process and verify that it exits
- `qzx killProcess 1234 --force --dangerously-bypass-approvals-and-sandbox`
  Force-kill PID 1234 when graceful termination is not appropriate

**Details:**
Terminate the requested process and wait for observable exit.

---

### listCommands

Lista todos los comandos disponibles en QZX, organizados por categoría.
Permite filtrar comandos por nombre o descripción.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Lists all available commands organized by category

**Parameters:**
- `filter_text`: Optional text to filter commands by name or description - Optional

**Examples:**
- `qzx listCommands`
  Lists all available commands organized by category
- `qzx listCommands file`
  Lists all commands containing 'file' in their name or description

**Details:**
Lists all available commands organized by category.

Args:
    filter_text (str, optional): Text to filter commands by name or description

Returns:
    dict: Dictionary containing list of commands organized by category

---

### listDiskDevices

Command to get disk name/model information

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Gets disk name/model information for a disk or all disks

**Parameters:**
- `disk_path`: Path to the disk to get information for. If not provided, shows information for all disks - Optional

**Examples:**
- `qzx listDiskDevices`
  Get information about all disks
- `qzx listDiskDevices C:`
  Get information about the C: drive (Windows)
- `qzx listDiskDevices /dev/sda`
  Get information about the /dev/sda disk (Linux)

**Details:**
Gets disk name/model information

Args:
    disk_path (str, optional): Path to the disk to get information for.
                              If not provided, shows information for all disks.

Returns:
    Dictionary with disk information

---

### listProcesses

Command to list running processes

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Lists running processes (similar to 'ps' in Unix)

**Parameters:**
- `filter_str`: Optional string to filter process names - Optional
- `sort_by`: Field to sort results by (pid, cpu, memory, name) - Optional (default: `cpu`)
- `limit`: Maximum number of processes to display - Optional (default: `0`)

**Examples:**
- `qzx listProcesses`
  List all processes
- `qzx listProcesses python`
  List processes containing "python" in their name
- `qzx listProcesses null memory 10`
  List the top 10 processes by memory usage

**Details:**
Lists running processes

Args:
    filter_str (str, optional): Optional string to filter process names
    sort_by (str, optional): Field to sort results by (pid, cpu, memory, name)
    limit (int, optional): Maximum number of processes to display

Returns:
    Dictionary with the process list and operation status

---

### listSystemServices

Inspect active and stopped services through the native manager.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Lists operating-system services and their running status

**Parameters:**
- `status`: Filter by service status (running, stopped, all; defaults to all) - Optional (default: `all`)

**Examples:**
- `qzx listSystemServices`
  List all system services
- `qzx listSystemServices running`
  List only active running system services

**Details:**
Method that executes the command

Args:
    *args: Positional arguments
    **kwargs: Keyword arguments

Returns:
    The result of the command execution

---

### runDiagnosticCommand

Execute one read-only diagnostic with a strict argument grammar.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Runs a strictly read-only native system diagnostic from a platform-specific allowlist

**Parameters:**
- `command`: Read-only diagnostic. Windows: hostname, ipconfig, netstat, whoami. Unix: cal, date, free, hostname, netstat, ss, uname, uptime, whoami - Required
- `args`: Arguments accepted by QZX's command-specific read-only grammar - Optional (default: `[]`)

**Examples:**
- `qzx runDiagnosticCommand hostname`
  Read the local system host name
- `qzx runDiagnosticCommand whoami`
  Read the current operating-system user
- `qzx runDiagnosticCommand uname -a`
  Read Unix kernel and architecture details
- `qzx runDiagnosticCommand ipconfig /all`
  Read the complete Windows network configuration
- `qzx runDiagnosticCommand netstat -an`
  List connections numerically without name resolution

**Details:**
Method that executes the command

Args:
    *args: Positional arguments
    **kwargs: Keyword arguments

Returns:
    The result of the command execution

---

### runScript

Execute a supported script without pretending to sandbox its behavior.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Executes one explicit Python, Bash, or Windows Batch script with a timeout and bounded retained output

**Parameters:**
- `script_path`: Path to the local script to execute - Required
- `args`: Arguments passed to the script; values are not echoed in the result - Optional (default: `[]`)

**Examples:**
- `qzx runScript myscript.py --yolo`
  Execute a reviewed Python script when its mutation targets cannot be determined
- `qzx runScript myscript.py arg1 arg2 --yolo`
  Execute with two arguments without repeating their values in the result
- `qzx runScript script.sh --dangerously-bypass-approvals-and-sandbox`
  Execute a reviewed Bash script without a safety backup

**Details:**
Execute a supported script and return a bounded structured result.

---

### systemDoctor

Command to run a comprehensive diagnostic of the host operating system.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Performs a complete diagnostic of the CPU, RAM, disks, network, PATH, services, ports, and system errors

**Parameters:**
- `quick`: If True, perform only a quick essential system check (default: False) - Optional (default: `false`)

**Examples:**
- `qzx systemDoctor`
  Run full system diagnostic
- `qzx systemDoctor --quick True`
  Run quick essential check

**Details:**
Executes the system diagnostic check

---

### systemInfo

Inspect portable host facts, with costlier sections on demand.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Gets portable operating-system, Python, user, and environment information with optional RAM and storage details

**Parameters:**
- `detailed`: Include RAM and storage details (true/false; defaults to false) - Optional (default: `false`)
- `include_environment`: Include selected local environment-variable values (true/false; defaults to false) - Optional (default: `false`)

**Examples:**
- `qzx systemInfo`
  Get a fast, portable system summary
- `qzx systemInfo --detailed`
  Add current RAM and storage details
- `qzx systemInfo --include-environment`
  Include selected local environment variables

**Details:**
Build the requested report without emitting side-effect output.

---

### terminal

Interactive terminal/shell for QZX commands

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Launches an interactive terminal/shell for QZX commands

**Parameters:**
- `prompt`: Custom prompt for the terminal (default: "QZX> ") - Optional (default: `QZX> `)
- `history_file`: Optional path to a persistent history file (disabled by default) - Optional
- `show_path`: Show path in the prompt (default: true) - Optional (default: `true`)

**Examples:**
- `qzx Terminal`
  Launch the QZX interactive terminal with default settings
- `qzx terminal "Agent> "`
  Launch the QZX interactive terminal with an agent prompt
- `qzx Terminal "MyQZX> "`
  Launch the QZX terminal with a custom prompt
- `qzx Terminal "QZX> " --history_file ~/.qzx_history --show_path false`
  Opt in to persistent history and hide the path

**Details:**
Launch an interactive terminal for QZX commands

Args:
    prompt (str, optional): Custom prompt for the terminal
    history_file (str, optional): Path to history file
    show_path (str, optional): Whether to show path in prompt ('true' or 'false')

Returns:
    Dictionary with the result of the operation

---

### testDiskSpeed

Measure sequential read and durable write throughput.

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Measures sequential durable-write and buffered-read throughput in MiB/s using a uniquely named temporary file

**Parameters:**
- `test_path`: Existing directory whose filesystem will be benchmarked - Optional (default: `.`)
- `size_mib`: Temporary fixture size in MiB, from 1 through 1024 - Optional (default: `50`)

**Examples:**
- `qzx testDiskSpeed`
  Benchmark the current filesystem with a 50 MiB fixture
- `qzx testDiskSpeed C:/temp --size-mib 100`
  Benchmark C:/temp with a unique 100 MiB temporary fixture

**Details:**
Run the benchmark and remove its unique fixture on every path.

---

### version

Command to display the current version of QZX

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Displays the current version of QZX and system information

**Parameters:**
None

**Examples:**
- `qzx version`
  Display the current version of QZX and system information
- `qzx --version`
  Display the current version using the global version flag

**Details:**
Displays the current version of QZX

Returns:
    Dictionary with version information

---

### welcome

Command to display the system welcome information

**Category:** system
**Maturity:** Alpha — Available for real use and feedback while its interface and behavior can still evolve.
**Description:** Displays the QZX welcome immediately, with optional system details

**Parameters:**
- `full_info`: Collect and show system, memory, and storage details; disabled by default for fast startup - Optional (default: `false`)

**Examples:**
- `qzx Welcome`
  Display the welcome screen immediately
- `qzx Welcome true`
  Collect system details, then display the detailed welcome screen

**Details:**
Display the welcome screen with system information

Args:
    full_info (str): Whether to show full information ('true' or 'false')

Returns:
    Dictionary with the operation result

---
